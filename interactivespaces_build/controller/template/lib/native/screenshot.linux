#!/bin/bash

SCREENSHOT_DIR=$1
SCREENSHOT_FILE=$2
SCREENSHOT_FILE_EXTENSION=$3

cd "${SCREENSHOT_DIR}"

OUTFILE="${SCREENSHOT_FILE}.${SCREENSHOT_FILE_EXTENSION}"

rm -f "${OUTFILE}"

export DISPLAY="${DISPLAY:-:0}"

xwd -root | convert xwd:- "${OUTFILE}"
